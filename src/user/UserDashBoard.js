import React from 'react'
import { isAuthenticated } from '../auth/helper'
import Base from '../core/Base'
const UserDashBoard = () => {
  return (
    <Base title="UserDashBoard page">  
    <h1>This is User Dash Board</h1>
    </Base>
  )
}

export default UserDashBoard;